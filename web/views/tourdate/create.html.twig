{% extends 'ag_base/backend.html.twig' %}

{% block container %}

    <div class="wrapper row-offcanvas row-offcanvas-left">

        {% include 'menu.html.twig' with {'option': 'tourdate_create'} %}

        <!-- Right side column. Contains the navbar and content of the page -->
        <aside class="right-side">                
            <!-- Content Header (Page header) -->
            <section class="content-header">
                <h1>
                    Create Tourdate
                </h1>
            </section>

            <!-- Main content -->
            <section class="content">

                {% include 'ag_alerts.html.twig' %}

                <div class="row">
                    <div class="col-md-6">

                        {{ form_start(form, {'method': 'POST'}) }}
                            {{ form_errors(form) }}

                            <div class="box box-info">
                                <div class="box-header">
                                    <h3 class="box-title"></h3>
                                </div><!-- /.box-header -->
                                <div class="box-body">

                                    <div class='form-group'>
                                        {{ form_label(form.tourdate_who) }}
                                        {{ form_widget(form.tourdate_who, { attr: { 'class': 'form-control' }}) }}
                                    </div>

                                    <div class='form-group'>
                                        {{ form_label(form.tourdate_when) }}
                                        {{ form_widget(form.tourdate_when, { attr: { 'class': 'form-control' }}) }}
                                    </div>

									<div class='form-group'>
									    {{ form_label(form.tourdate_where) }}
									    {{ form_widget(form.tourdate_where, { attr: { 'class': 'form-control' }}) }}
									</div>

									<div class='form-group'>
									    {{ form_label(form.tourdate_link) }}
									    {{ form_widget(form.tourdate_link, { attr: { 'class': 'form-control' }}) }}
									</div>

									<div class='form-group'>
									    {{ form_label(form.tourdate_published) }}
									    {{ form_widget(form.tourdate_published, { attr: { 'class': 'form-control' }}) }}
									</div>

                                </div><!-- /.box-body -->

                                {{ form_rest(form) }}

                                <div class="box-footer">
                                    <button type="submit" class="btn btn-primary">Create</button>
                                </div>

                            </div><!-- /.box -->
                        {{ form_end(form) }}


                    </div>
                </div>

            </section><!-- /.content -->
        </aside><!-- /.right-side -->
    </div><!-- ./wrapper -->

{% endblock %}
